document.addEventListener('DOMContentLoaded', () => {
    window.loadChat('btn__submit', 'chat__input');
    window.reaction('btn__submit', 'chat__input');

    // アクセス時にinfoのデータ取得する
    // アクセス時にinfoの接続を行う
});